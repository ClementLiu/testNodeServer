extends layout

block content
  h1= title
  form#formAddImg(name="addimgs",method="post",action="/upload",enctype="multipart/form-data") Select an image to upload:
    input#inputImgName(type="file", name="image")
    button#btnSubmit(type="submit",value="Upload Image") submit